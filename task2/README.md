# README

## Running

`make cycle` and in another terminal `make run`

`make run` will output the DAG steps for visualization.

Additionally, you can run `make cycle` from the `../task3` directory, and then run `make run` to view the error output and cancellation propagation and exponential backoff. 