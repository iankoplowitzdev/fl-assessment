# README

## Running

`make cycle` and in another terminal `make run`

`make run` will run the demo binary, which shows the server middleware performing its jobs, including the auth and rate limiting.